import { bindActionCreators } from 'redux';
import { connect } from 'react-redux';
import Player from '../components/Player'
// import actions

// Needs mapStateToProps(state) and mapDispatchToProps(dispatch)
